<p class="actvity-item">
  <%= a.trackable.goal.username %><%= a.trackable.username %> added a goal of <%= a.trackable.num_sessions %> <%= a.trackable. session_length%> minute session(s) for the following skill: <%= a.trackable.skill.nickname %>.
</p>

require 'rails_helper'

RSpec.feature "User sees recent activity" do
  scenario "user sees activity" do
    #from dashboard, i click on activity
    #i visit /activity
    #i see a list of 20 most recent goal setting, skill adding, and session logging activities in reverse chron order



  end
  

end
